# Vaste, geteste versies voor de Energy-Truth worker (reproduceerbare builds).
# Gegenereerd uit een werkende omgeving (pip freeze, 31 mei 2026).
#
# Bewust EXACT gepind (==): elke build, elke machine en de k8s-pod krijgen
# precies dezelfde versies. Updaten doe je bewust: versie ophogen, testen,
# en daarna deze pins bijwerken (niet automatisch de laatste pakken).
#
# Installeren:  pip install -r requirements.txt
#
# Let op: de code importeert "psycopg2"; die module wordt geleverd door het
# pakket psycopg2-binary (handig, geen build-tools nodig).

matplotlib==3.10.9
numpy==2.4.4
pandas==3.0.2
psycopg2-binary==2.9.12
reportlab==4.4.10
