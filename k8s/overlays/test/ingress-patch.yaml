apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
spec:
  rules:
    - host: test.energytruth.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-frontend-service
                port:
                  number: 80
