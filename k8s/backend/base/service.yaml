apiVersion: v1
kind: Service
metadata:
  name: energytruth-backend-service
spec:
  selector:
    app: energytruth-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
