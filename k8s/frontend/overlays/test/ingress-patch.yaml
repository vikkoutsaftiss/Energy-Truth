apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: test.energy-truth.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: energytruth-frontend-service
                port:
                  number: 80
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: energytruth-backend-service
                port:
                  number: 80
