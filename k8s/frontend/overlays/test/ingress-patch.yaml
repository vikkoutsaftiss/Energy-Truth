apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
spec:
  rules:
    - host: test.energy-truth.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: test-energytruth-frontend-service
                port:
                  number: 80
