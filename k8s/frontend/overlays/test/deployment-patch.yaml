apiVersion: apps/v1
kind: Deployment
metadata:
  name: energytruth-frontend
  namespace: energytruth-test
spec:
  template:
    spec:
      containers:
        - name: frontend
          ports:
            - containerPort: 80
          env:
            - name: ASPNETCORE_URLS
              value: "http://+:80"
