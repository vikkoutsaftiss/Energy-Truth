apiVersion: v1
kind: Service
metadata:
  name: energytruth-frontend-service
spec:
  selector:
    app: energytruth-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
